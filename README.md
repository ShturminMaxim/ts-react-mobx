# webpack typescript react mobx

Stack I would choose starting project today

## Usage
Run `npm run dev` to start webpack dev server.

Run `npm run build` to get minified output file `dist/bundle.js`
